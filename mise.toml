[tools]
python = "3.14.0"
uv = "latest"

[settings]
python.uv_venv_auto = true

[tasks.query-API]
description = 'Start CLI Agent'
run = "uv run main.py"

[tasks.dry-run]
description = 'Run local test without API'
run = "uv run test.py"
